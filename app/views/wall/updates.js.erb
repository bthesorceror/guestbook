<% @posts.each do |post| %>
	var item = $("<%=raw escape_javascript(render('post', :post => post)) %>");
	$("#wall").prepend(item);
	$( "#post_<%= post.id %>" ).effect( 'highlight', {}, 2000);
<% end %>
